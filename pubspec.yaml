name: hae_decision_framework
description: "HAE Sokratik Karar Laboratuvarı: Bilişsel filtreleme ve anti-öz-aldatma motoru."
publish_to: 'none'

version: 2.3.0+4

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5
  intl: ^0.20.3
  http: ^1.6.0
  flutter_markdown: ^0.7.7+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true